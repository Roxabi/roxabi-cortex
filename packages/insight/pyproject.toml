[project]
name = "cortex-insight"
version = "0.1.0"
description = "cortex-insight — lake + ETL service for roxabi-cortex"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cortex-insight = "cortex_insight.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cortex_insight"]
