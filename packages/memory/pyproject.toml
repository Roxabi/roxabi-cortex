[project]
name = "cortex-memory"
version = "0.1.0"
description = "cortex-memory — warehouse + consolidation service for roxabi-cortex"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cortex-memory = "cortex_memory.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cortex_memory"]
